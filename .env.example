# 서버 환경 설정
PORT=3000
NODE_ENV=production

# 데이터베이스 경로
# 서버/GCP/마이그레이션/운영 스크립트에서는 필수입니다.
# 데스크톱 앱은 Electron userData/exocad.db를 기본 사용합니다.
DB_PATH=/home/geomedicho/exocad-serial/data/exocad.db

# 로그 디렉토리 (기본값: process.cwd()/logs)
# LOG_DIR=/home/geomedicho/exocad-serial/logs
